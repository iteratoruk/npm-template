{
	"name": "npm-template",
	"version": "1.0.0",
	"description": "Skeleton for npm-based javascript projects",
	"main": "src/js/app.js",
	"scripts": {
		"test": "echo \"Error: no test script configured\" && exit 1",
		"test:watch": "echo \"Error: no test script configured\" && exit 1",
		"start": "echo \"Error: no server script configured\" && exit 1",
		"build": "webpack",
		"lint": "echo \"Error: no lint script configured\" && exit 1",
		"clean": "echo \"Error: no clean script configured\" && exit 1"
	},
	"repository": {
		"type": "git",
		"url": "git@github.com:iteratoruk/npm-template.git"
	},
	"keywords": [
		"npm",
		"template",
		"skeleton",
		"archetype",
		"es6",
		"webpack",
		"mocha",
		"karma",
		"phantomjs",
		"bootstrap"
	],
	"author": "Christopher Townson",
	"license": "Apache-2.0",
	"bugs": {
		"url": "https://github.com/iteratoruk/npm-template/issues"
	},
	"homepage": "https://github.com/iteratoruk/npm-template#readme",
	"devDependencies": {
		"webpack": "^3.8.1"
	}
}
